# torch / torchvision come from the CUDA base image — do NOT reinstall here.

# diffusers 0.34.0: has WanVACEPipeline (reference_images) + CausVid LoRA loading,
# but predates the attention_dispatch flash-attn-3 custom_op that breaks import on
# torch < 2.6 (the RunPod base image torch). Do NOT bump to >=0.35 unless torch is
# also upgraded to >=2.6 (see README "Troubleshooting").
diffusers==0.34.0
transformers>=4.49.0,<4.54
accelerate>=1.4.0
peft>=0.14.0
safetensors>=0.4.5
sentencepiece>=0.2.0          # umt5-xxl tokenizer
ftfy>=6.2.0

# serving
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18

# media io
imageio>=2.36.0
av>=13.0.0                    # pyav backend for imageio (decode/encode mp4)
pillow>=10.4.0
numpy>=1.26,<2.3

# model download
huggingface_hub>=0.27.0
hf_transfer>=0.1.8
