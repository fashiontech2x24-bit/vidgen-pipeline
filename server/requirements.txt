# torch / torchvision come from the CUDA base image — do NOT reinstall here.

# diffusers main: WanVACEPipeline (reference_images) + Wan LoRA auto-conversion
# (CausVid). Pin to a commit once validated on RunPod for reproducibility.
diffusers @ git+https://github.com/huggingface/diffusers.git@main
transformers>=4.49.0
accelerate>=1.4.0
peft>=0.14.0
safetensors>=0.4.5
sentencepiece>=0.2.0          # umt5-xxl tokenizer
ftfy>=6.2.0

# serving
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18

# media io
imageio>=2.36.0
av>=13.0.0                    # pyav backend for imageio (decode/encode mp4)
pillow>=10.4.0
numpy>=1.26,<2.3

# model download
huggingface_hub>=0.27.0
hf_transfer>=0.1.8
